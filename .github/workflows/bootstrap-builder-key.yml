name: Bootstrap encrypted builder key

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Restore builder private key
        id: keycache
        uses: actions/cache/restore@v4
        with:
          path: .builder-private.pem
          key: daftarqarzsafeen-builder-rsa-v1

      - name: Generate builder private key
        if: steps.keycache.outputs.cache-hit != 'true'
        run: |
          openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:3072 -out .builder-private.pem
          chmod 600 .builder-private.pem

      - name: Save builder private key
        if: steps.keycache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: .builder-private.pem
          key: daftarqarzsafeen-builder-rsa-v1

      - name: Publish public modulus only
        run: |
          set -euo pipefail
          openssl rsa -in .builder-private.pem -noout -modulus | sed 's/^Modulus=//' > builder-modulus.hex
          printf '%s\n' '65537' > builder-exponent.txt
          git config user.name "Encrypted Builder Bootstrap"
          git config user.email "builder@localhost"
          git add builder-modulus.hex builder-exponent.txt
          if ! git diff --cached --quiet; then
            git commit -m "Publish encrypted builder public key"
            git push origin HEAD:main
          fi
